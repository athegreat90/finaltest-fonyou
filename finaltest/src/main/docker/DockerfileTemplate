FROM openjdk:8-jdk-alpine
VOLUME /tmp
COPY maven/${filename}.jar ${filename}.jar
RUN sh -c 'touch /myapp.jar'
ENTRYPOINT ["java","-Djava.security.egd=file:/dev/./urandom","-jar","/${filename}.jar"]
EXPOSE 8080